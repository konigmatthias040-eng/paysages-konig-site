# backups locaux — ne pas publier
index.html.bak*
*.bak

# photos AVANT floutage (RGPD) — ne JAMAIS publier
images/_originals/

# divers
.DS_Store
Thumbs.db
*.zip

# outils locaux (aperçu) — ne pas publier
.claude/
_cap*.html
